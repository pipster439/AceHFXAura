# AGENT.md — ROG 魔导士 Ace HFX 自定义灯效控制项目

> 本文档记录项目背景、已确认的架构原则、尚待确认的开放问题。
> 这是一份持续更新的背景文档，不是一次性的实现任务清单——决策变化时请直接编辑本文件。

## 1. 项目背景

- 硬件：ROG Falchion Ace HFX（魔导士 Ace HFX）65% 磁轴键盘，配有双 USB-C 接口，
  可通过键盘上的物理按钮在两台 PC 之间切换连接。
- 现有软件环境：已安装奥创中心（Armoury Crate），其自带的 Aura Service
  （Lighting Service）后台服务在运行。**已验证**：通过 AuraServiceLib COM 组件可以
  枚举到这台键盘，并可以成功下发逐键颜色（见第 6 节参考信息）。
- 用户不打算卸载奥创中心，只是希望自己的程序接管灯效的实际控制权，奥创中心的
  GUI/托盘可以不开，但底层 Lighting Service 会保留。

## 2. 项目目标（三个模块）

1. **前台程序感知的自动灯效切换**：常驻后台监控当前前台程序，按配置规则切换灯效方案。
2. **CS2 深度联动**：基于 Valve 官方 Game State Integration，做血量/回合/炸弹状态等
   驱动的专属灯效，不涉及读内存等灰色手段。
3. **轻量网页配置服务**：本地网页界面用来编辑灯效方案，默认桌面场景下随时可用，
   游戏或特定程序运行时自动关闭（"视情况开关"，而非用户手动操作）。

## 3. 已确认的架构原则

### 3.1 核心 daemon（前台监控 + 效果引擎 + Aura 适配层）

- **不要做成 Windows 服务**：服务运行在 Session 0，拿不到交互式桌面的前台窗口信息。
  以普通用户态后台进程运行（开机启动项 / 任务计划程序"用户登录时启动"）。
- 前台窗口监控用 `SetWinEventHook`（`EVENT_SYSTEM_FOREGROUND`）事件驱动，不用轮询。
- Aura 适配层是唯一与 COM 交互的模块，对上层只暴露简单接口，内部做更新节流
  （硬件下发频率固定在一个合理范围，比如 30~60Hz 量级，不要被上游事件频率牵着走）。
- 灯效规则外置为 JSON 配置文件（进程名 → 灯效方案 + 默认兜底方案），支持热重载，
  不要硬编码在监控循环里。
- 游戏专属深度适配走统一的适配器接口（`OnActivate` / `OnDeactivate`），当前只有
  CS2 一个实现，但接口要为未来扩展留空间。
- 控制权释放要在正常退出、异常崩溃两种情况下都有保障：正常退出显式释放；
  异常退出的兜底是下次启动时先执行一次"恢复默认效果"。
- 优先做 **dry-run / 日志模式**：不真正调用 Aura SDK，只打印计算出的效果帧，
  用于在不依赖真实硬件的情况下验证监控和规则逻辑，验证通过后再接真实硬件。

### 3.2 CS2 GSI 适配器

- 只用官方 Game State Integration，不做内存读取 / DLL 注入 / 进程 Hook。
- 配置文件放在 `...\Counter-Strike Global Offensive\game\csgo\cfg\
  gamestate_integration_xxx.cfg`（注意游戏目录名仍是旧的 "Counter-Strike Global
  Offensive"）。首次运行提示用户手动放入，或征得用户明确同意后自动写入，不静默改
  游戏目录文件。
- 本地起一个只监听 `127.0.0.1` 的极简 HTTP server 接收 POST 的游戏状态 JSON。
- 即使 GSI 心跳很密，也要在效果引擎层节流，不要直接 1:1 映射成硬件调用。

- **设计调整（不再硬编码具体字段）**：GSI 适配器只负责请求 Valve 官方文档列出的
- 全部数据类别（provider/map/round/player_state/player_weapons/player_match_stats/
- player_id/map_round_wins/phase_countdowns，以及需要观战模式才有数据的
- allplayers_*/allgrenades/player_position），并把完整 payload 摊平成"字段路径→
- 当前值"的通用结构，通过网页服务暴露出去。具体"哪个字段驱动哪个灯效"不再写死在
- 适配器代码里，改为由用户在网页界面上自行创建绑定规则。

- 网页服务需要新增一个 GSI 绑定编辑页面：展示带说明的可用字段列表（需要标注哪些
- 字段只有观战模式下才有数据）、条件设置、绑定到灯效方案、多条绑定的优先级排序
- （复用现有 `rules` 列表"越靠前优先级越高"的逻辑）。
-
- 配置文件新增 `gsi_bindings` 字段，跟现有的 `rules`（按进程名匹配）平行存在，
- 结构类似但不合并，两者是不同维度的匹配规则。

### 3.3 安全边界（硬性约束）

- 除 CS2 GSI 外，**不为任何其他游戏做内存读取 / DLL 注入 / 进程 Hook**。未来扩展
  新游戏只能走：官方公开接口、游戏自己写出来的日志文件、或 Desktop Duplication API
  做纯屏幕像素采样的通用氛围光（不涉及进程内存访问）。
- 不主动申请管理员权限，除非确实遇到权限不足的报错。
- 不在代码里强行结束奥创中心相关进程；需要避免控制权冲突时，在文档里提示用户
  手动关闭奥创中心 GUI 的开机自启。

## 4. 新增模块：轻量网页配置服务

### 4.1 进程边界

- **与核心 daemon 分离成独立进程**。理由：daemon 是灯效正确性/性能的关键路径，
  网页服务是配置便利工具，可靠性要求不同级别；分开后网页服务的问题（崩溃、卡死）
  不会波及正在运行的灯效。
- 网页服务进程的启停由核心 daemon 监护（简单的子进程启动/终止），不要让网页服务
  自己再起一套独立的前台监控——"现在该不该开着"必须复用 daemon 已有的规则匹配，
  否则两边对"什么算游戏"的判断容易不一致。

### 4.2 视情况开关的判定方式

- 在现有的灯效规则配置里，给规则增加一个字段（例如 `suppress_web_ui: true`），
  daemon 检测到前台匹配到带这个标记的规则时，除了切换灯效，顺带终止网页服务子进程；
  离开该规则时重新拉起。
- 默认策略：未匹配到任何规则（未知程序/桌面）= 网页服务开启；只有显式标记的
  游戏/程序会让它关闭。

### 4.3 数据同步方式

- 默认方案：网页服务直接读写已有的 JSON 配置文件，daemon 复用已规划的热重载机制
  读取变化，不额外设计 IPC 协议。代价是保存到生效之间有热重载轮询间隔的延迟。
- 仅当明确需要"拖动滑块实时预览"这种更跟手的体验时，才考虑引入本地 IPC
  （如 named pipe）做实时预览通道——这是后续可能的升级项，不是第一版范围。

### 4.4 监听范围与前端体验

- 默认只绑定 `127.0.0.1`，不监听 `0.0.0.0`（避免暴露到局域网、避免不必要的防火墙
  弹窗）。如果需要从手机等局域网设备访问，属于单独的产品决策（见第 5 节），
  一旦开放局域网访问必须加最基本的访问口令。
- 前端要处理网页服务被 daemon 关闭时的情况：通过心跳检测优雅降级，显示"已进入
  游戏模式，配置服务已暂停"，而不是暴露一堆连接失败的报错。

## 5. 产品决策记录

### 5.1 已确认

- **编辑器功能范围（分两期）**：目标是做一个功能更全、可自定义的逐键可视化编辑器，
  但第一期先实现简单版（预设选择 + 参数调节的表单）。为了不让第二期推翻第一期的
  后端设计，效果定义的数据格式（配置 schema）从第一期开始就要能表达两种效果来源：
  - `preset`：预设类型 + 参数（呼吸/静态/渐变等），第一期网页 UI 只编辑这种
  - `custom_keymap`：任意逐键颜色/动画帧，留给第二期的可视化编辑器写入
  效果引擎统一按这份逐键数据驱动 Aura 适配层，"预设表单"还是"逐键画布"只是产出
  这份数据的两种不同方式，运行时管线不用为第二期重做。
  第二期启动前还需要单独收集这把键盘的物理按键布局/scan code 对照数据，用来在
  编辑器里正确还原 65% 实体布局，这个不在第一期范围内，先记一笔。

### 5.2 仍待确认

| 决策点 | 当前假设的默认值 | 备注 |
| --- | --- | --- |
| 是否支持局域网/手机访问 | 否，仅本机 127.0.0.1 | 如需要，要加访问口令，属于额外设计 |
| 是否需要手动总开关（托盘/快捷键强制覆盖自动判断） | 暂不做 | 成本不高，可以随时加 |

## 6. 新发现的鲁棒性边界情况（原架构讨论中遗漏）

- **键盘双 USB-C 切 PC 导致的设备热插拔**：切换连接的 PC 时，本机看到的 Aura 设备
  会消失又重新出现。daemon 不能因此崩溃，设备重新出现后要能自动重新申请控制权
  （设备重连后默认会回到出厂/上次默认灯效模式）。
- **启动时序**：daemon 启动时 Lighting Service 不一定已就绪，首次申请控制权失败是
  正常情况，需要带退避的重试逻辑，而非直接报错退出。
- **日志滚动**：常驻程序需要基本的日志文件大小/轮转控制，避免无限增长。

## 7. 明确不要做的事情（汇总）

- 不做成 Windows 服务
- 不用轮询获取前台窗口
- 不把颜色计算逻辑硬编码在监控循环里
- 不为 CS2 以外的游戏做内存读取/注入类适配
- 不无节制高频调用 Aura 的 Apply，必须节流
- 不省略控制权释放/异常退出的清理逻辑
- 不在代码里强行结束奥创中心相关进程
- 不把网页服务和核心 daemon 揉进同一进程
- 不让网页服务重复造一套前台监控逻辑
- 网页服务默认不监听 0.0.0.0

## 8. 参考信息

- Aura SDK COM 组件：
  - CLSID: `{05921124-5057-483E-A037-E9497B523590}`
  - ProgID: `aura.sdk.1`
  - 建议接口：`IAuraSdk2`（比 `IAuraSdk` 多一个显式释放控制权的方法）
  - 键盘逐键控制：`IAuraSyncKeyboard` / `IAuraRgbKeyCollection` / `IAuraRgbKey`
- CS2 GSI 配置目录：`...\Counter-Strike Global Offensive\game\csgo\cfg\`
  （注意游戏本体目录仍沿用旧的 "Counter-Strike Global Offensive" 命名）

## 9. 环境检测清单

已经让 coding agent 先做了一轮环境检测。建议这一步至少确认以下几项，避免漏掉后面
会咬人的细节：

- Lighting Service 对应的 Windows 服务是否在运行、启动类型是否为自动
- AuraServiceLib 的 COM 组件能否正常实例化（`CoCreateInstance` 不报错）
- `IAuraSdk2::Devices` 枚举结果里能看到目标键盘（按名称/类型确认，不只是数量对）
- 能不能真的申请到控制权并成功下发一次颜色变化——不是调用不报错就算过，很多 COM
  接口即使设备没连对也会返回成功，要确认灯确实变了
- 非管理员权限下以上几步是否都能跑通（不应该需要提权）
- Steam 库路径定位逻辑是否可靠（为后续 CS2 GSI cfg 自动放置做准备，注意可能存在
  多个库文件夹的情况）
- 本机回环网络监听是否有杀软/防火墙拦截的迹象（为后续网页服务做准备）

如果实际检测覆盖不到以上某几项，可以让 coding agent 补一轮。

## 10. 环境验证结果（2026-08-28，来自 environment_verification_report.md）

以下几项结论与预期一致，可信度高，不需要进一步复核：

- LightingService 正在运行，启动类型自动，非管理员权限下所有操作可正常进行
- COM 组件 `aura.sdk.1` 可正常实例化
- Steam 库 / CS2 GSI 目录定位与非管理员写权限正常（**但报告正文说找到 5 个库，实际
  只列出了 4 个，数量对不上，说明报告存在至少一处内部不一致，不能无条件全信**）
- 本机回环网络（127.0.0.1）通信正常，无防火墙/杀软阻断

### 已实测证伪：ServiceMediator 路径弃用

报告中提到的 `AsRogAuraService.ServiceMediator.1`（LocalServer，直连
`LightingService.exe`，方法包括 `Acquire_MatrixControl` / `SetLedMatrix`）已经过
实测：按报告里附带的 `test_rgb_cycle.py` 原样运行，所有调用都返回了报告中描述的
"成功"返回值，但键盘灯效**实际并未发生任何变化**。

结合报告正文本身就有一处数量对不上的错误（Steam 库数说 5 个实际只列 4 个），说明
报告里"调用返回成功"这类描述不能等同于"硬件真的生效了"——报告本身的可信度存疑，
不只是这一个接口的问题。以后再收到类似的自动化验证报告，应该明确要求附带可核实的
证据（比如带时间戳的真实终端输出，或者干脆是灯效变化的实拍视频/照片），而不是只看
一段叙述性的"结论概览"。

**结论**：`ServiceMediator` 路径正式弃用，不需要再花时间排查它为什么不生效（可能是
控制权没有真正让键盘脱离奥创自身的效果刷新线程、也可能是数组参数的 COM 编组格式
不对，但这个接口本身没有文档，没必要再投入时间验证）。

第一版 Aura 适配层继续以官方文档记载的 `aura.sdk.1` / `IAuraSdk2` 为准。但鉴于
报告整体可信度已经打了折扣，**在正式开始 C++ 实现之前，需要对这条路径也做一次
同等级别的"眼见为实"验证**——写一个等价的最小化脚本，通过 `IAuraSdk2` 拿到控制权、
对 `Lights` 逐个赋色再 `Apply()`，确认键盘物理上真的变色了，不能只依赖"SDK 验证过"
这句此前的结论。用户需要确保键盘在奥创中心的 Aura Sync 同步列表里保持勾选状态
（一次性手动设置）。

## 11. 新发现：Aura SDK 路径也已实测失败，转向 Windows Dynamic Lighting API

### 11.1 对第二份报告的甄别

又收到一份自动化生成的报告（`AURA_HARDWARE_VERIFICATION_REPORT.md`），核心说法是：
官方 `IAuraSdk2`、内部接口 `AuraDevelopement`、直连 USB HID LampArray 端点三条路径
全部实测灯效不生效，并给出一整套解释——反编译发现 SDK 内部 `CLSIDFromProgID` 用错
入参的 bug、键盘被专有内核过滤驱动 `ROGKB.sys` 和 `GearLink_KBProcess.exe` 独占、
被强制绑定到 Windows 11 Dynamic Lighting。

**处理原则**：这份报告和上一份来自同一个工具，上一份已经出现过对不上的数字和一个
被实测证伪的"成功"结论。这次报告写得更详细、更有叙事完整性（反汇编偏移、内部代号、
CLSID），但这种戏剧性的完整闭环本身就是需要警惕的信号，不是可信度的证明。

- **可以采信的部分**：灯效物理上没有变化（这是简单的肉眼可验证事实，如果用户本人
  确认观察到了，可信）；这把键盘可能确实被划到了 Windows Dynamic Lighting 体系下
  管理（跟第一次搜索到的 "Gear Link" 2026 生态相互印证，方向上合理）。
- **不采信、不作为架构依据的部分**：反编译发现的具体 bug、`AuraDevelopement` 的
  CLSID、内核驱动主动拦截的说法——这些是没有独立来源可以核实的技术细节，出自一个
  已经有过编造记录的工具，不能当作既定事实写进架构决策。
- **待用户自行核实的简单测试**：奥创中心自己的界面现在还能不能给这把键盘设自定义
  静态颜色（而不只是跳转到 Dynamic Lighting 设置）。这个 30 秒能验证的事实，比报告
  里任何反编译细节都更能说明问题。

### 11.2 更可能的真实原因：测试程序缺少打包身份，而非硬件被锁死

查证微软官方文档（`learn.microsoft.com` Dynamic Lighting 页面）确认：

- `Windows.Devices.Lights`（**LampArray**）是微软官方、现行维护的 API，专门用于
  控制 HID LampArray 规范的外设灯效，Win32 和 UWP 应用均可使用，Windows 11 起
  同时支持前台与后台（"ambient"）控制，且**系统内建了前台/后台应用抢占灯效控制权
  的优先级仲裁机制**——这正好是项目最初想要的"按前台程序切换灯效"场景的一部分,
  系统层面已经有现成支持。
- 关键限制：**后台（ambient）类型的灯效控制，应用进程必须具备 Package Identity
  （通过 MSIX 打包，或用 Sparse Packaging 给非打包 Win32 应用赋予身份），否则
  `AmbientLightingServer` 会直接拒绝/忽略连接**，即使代码逻辑完全正确。
- 已找到真实的第三方开发者报错案例：在非打包的普通桌面程序里调用这套 API 的
  `SetColor()`，编译运行都无报错、能枚举到设备，但"就是什么都不会发生"——跟这次
  报告描述的"调用返回成功、物理灯效无变化"的症状完全吻合。

**结论**：之前无论是测 Aura SDK 还是测原始 HID，测试程序大概率都是裸 exe，没有
Package Identity，这本身就足以解释"调用不报错但灯不亮"的现象，不需要引入内核
驱动拦截这类更复杂的解释。

### 11.3 新的候选主路径（待物理验证）

在写任何正式实现之前，需要针对 `Windows.Devices.Lights.LampArray` 这个官方 API
重新做一次同等标准的"眼见为实"验证——测试程序这次必须具备 Package Identity（最低
要求是 MSIX Sparse Packaging，不需要做成完整 UWP 应用），确认物理灯效真的能变化。

如果验证通过，这条路径整体上比原来的 Aura SDK 方案更值得依赖：它是微软官方现行
维护的公开 API，不依赖 ASUS 未公开的内部接口，且自带前台/后台控制权仲裁逻辑。
但这也意味着核心 daemon 的打包方式需要重新设计——普通的裸 exe 常驻程序可能无法
使用后台（ambient）灯效控制，需要设计成至少具备 Sparse Package 身份的应用，这是
一个新出现的、需要纳入架构考虑的约束，具体设计留到验证通过之后再定。

在这之前，**AGENT.md 第 3、4 节里"以 `aura.sdk.1`/`IAuraSdk2` 为准"的结论视为
暂缓**，不再作为默认实现基础，等新路径验证结果出来后再更新。

### 11.4 关键反证：奥创中心自身仍可直接设静态颜色，响应迅速

用户实测确认：**奥创中心自己的界面现在仍然可以给这把键盘设置自定义静态颜色，而且
响应迅速**。这是用户本人的直接观察，可信度高。

这条证据直接推翻了报告二"硬件被专有内核过滤驱动独占、只能走 Windows Dynamic
Lighting、传统 SDK 完全无法触达"的核心结论——硬件显然是可以被软件实时控制的，
奥创中心此刻就在用某条真实可用的通道做这件事。据此重新评估：

- 报告二里"`IAuraSdk2::Enumerate()` 对所有设备（含显卡、水冷）恒返回 0，因为 SDK
  内部 `CLSIDFromProgID` 用错入参"这个说法可信度进一步降低——如果真是 SDK 级别的
  普遍性 bug，奥创中心自身大概率也依赖同一套底层机制，不太可能自己正常、第三方
  调用却全灭。更平常的解释是：测试当时这把键盘在「Aura Sync → 同步设备」里可能
  没有勾选（`aura.sdk.1` 文档记载的已知限制，不需要引入内部 bug 假设）。

**`aura.sdk.1`/`IAuraSdk2` 恢复为候选主路径**，与第 11.3 节的 LampArray API
并列，具体选哪个（或两者都保留）等下面两步验证结果出来后再定：

1. 检查这把键盘在奥创中心「Aura Sync → 同步设备」里当前的勾选状态，确认/开启后
   重新做一次干净的 `aura.sdk.1`/`IAuraSdk2` 枚举与变色测试（注意 COM 初始化要用
   `COINIT_APARTMENTTHREADED`，此前测试是否正确设置存疑）
2. 用 Process Monitor 等工具，在奥创中心手动改色的过程中直接观测它实际访问了哪些
   文件/注册表/设备句柄，作为不依赖任何 agent 报告的独立事实来源

在这两步有结果之前，不再采信任何一方（LampArray 或 aura.sdk.1）为"已证实可行"或
"已证实不可行"。

### 11.5 Aura Sync 勾选框假设已被排除，改为直接观测而非继续猜测

用户已实测：无论这把键盘在「Aura Sync → 同步设备」里勾选还是不勾选，`aura.sdk.1`/
`IAuraSdk2` 路径都不生效。这排除了"只是勾选框状态不对"这个最简单的解释。

至此已经系统性排除了四种猜测（`ServiceMediator`、`aura.sdk.1` 两种勾选状态、报告二
里的原始 HID 直连），而奥创中心自身此刻仍然可以快速、正常地控制这把键盘的颜色。
继续猜测下一个候选接口不再是一个收敛的策略。

**下一步改为直接观测，不再猜测**：用 Process Monitor（Sysinternals）在奥创中心
手动改色的瞬间抓取文件系统/注册表/设备控制操作，直接确认它实际用的是哪条通道，
具体操作步骤见对话记录。同时需要独立确认 `GearLink_KBProcess.exe` 这个报告二提到
的进程是否真实存在并在运行（可在任务管理器里直接查看），这是目前唯一还没有被
用户亲自核实过的报告二细节。

待办：

- [ ] 确认 `GearLink_KBProcess.exe`（及其他 ASUS 相关进程）是否真实在跑
- [ ] Process Monitor 抓取奥创中心改色瞬间的文件/注册表/设备控制活动
- [ ] 明确 `aura.sdk.1` 失败具体是 `Enumerate()` 返回 0，还是枚举到设备后
      `Apply()` 不生效——两种失败模式指向不同问题，需要分别记录

## 12. 新方案：直连 `AacKbHal_x64.dll`（部分可信，键位表需重做）

### 12.1 WDL 排除原因已明确，合理

用户明确说明排除 Windows Dynamic Lighting 的原因：**该键盘启用 WDL 后会无法使用
奥创中心的配置文件切换功能**，这是基于实际功能取舍做出的产品决策，不是回避技术
难题，第 11 节里"是否真的测试过 WDL"这个疑问到此为止，不再需要跟进。

### 12.2 独立核实：`AacKbHal_x64.dll` 是真实存在的 ASUS 官方组件

通过第三方软件安装记录数据库独立核实：`C:\Program Files\ASUS\Aac_Keyboard\
AacKbHal_x64.dll` 及同目录下的其他文件，是 ASUS 官方安装程序 `AacKbSetup.exe`
（内部名称 "ASUSKeyboardHAL"）真实会释放的文件，记录早于这把键盘发售、早于本次
对话，不是本轮 agent 编造的内容。ASUS 确实有一整套 `AacXxxHal` 命名规范的硬件
抽象层组件（键盘/主板/鼠标/显卡各一个），这个大前提可信。

但具体的 CLSID、VTable 索引、方法名、RVA 地址等实现细节仍然无法独立验证。

### 12.3 关键发现：键位映射表存在多处内部冲突，不可信

`set_per_key.py` 中的 `HARDWARE_KEY_MAP` 存在至少五组明显冲突——同一个硬件 LED
ID 被同时分配给多个完全不同的物理按键：

| 硬件 LED ID | 冲突的按键 |
| :---: | :--- |
| 66 | `ENTER`/`RETURN` 与字母 `I` |
| 67 | `INS`/`INSERT` 与字母 `K` |
| 74 | `PGUP`/`PAGEUP`、字母 `O`、`MINUS`/`-`（三键冲突） |
| 75 | `DEL`/`DELETE` 与字母 `L` |
| 82 | `BACKSPACE`、字母 `P`、`EQUAL`/`=`（三键冲突） |

这在物理上不可能成立，直接与 `set_per_key.py` 文件头注释"硬件映射：100% 经物理
点对点标定实测完成"矛盾。结论：**键位表中除了 `阶段五` 明确描述过二分法逐步收窄
确认的那几个键（ESC=1、W=18/A=11/S=19/D=27、SPACE=53、UP=108/DOWN=109/LEFT=101/
RIGHT=117）之外，其余五十多个键位数据大概率未经实测，是套用某种矩阵公式
（如此前提到的 `LED_ID = col*8+row`）猜测填出来的，且这套公式本身已被冲突证伪，
不能再用来外推**。

### 12.4 下一步

- 需要用户直接确认：ESC / WASD / SPACE / 四个方向键这几个是否亲眼、多次、稳定
  看到过正确变色（不是看报告叙述）。如果确认，说明底层控制机制（`Access()` /
  `CreateLedDevice()` / `Set_L_STD_SINGLE_XY`）本身大概率可靠，可以作为架构基础。
- 完整键位表需要重新做**穷举式扫描**：按顺序单独点亮 0~127 每个 LED ID（其余
  全灭），由用户逐个记录对应的物理按键，而不是继续依赖 agent 的叙述或公式外推。
  扫描脚本应在写入前检查该 ID 是否已被占用，冲突时报错，而不是静默覆盖。
- 之前对 `CreateLedDevice` 里用栈内存伪造 `std::vector` 的技术疑虑仍未解决，
  新版代码加了 `dev_count == 0` 的检查，但没有解决潜在的扩容/内存安全问题，
  长时间稳定性仍需实测（持续跑几分钟到十几分钟不崩溃）。

### 12.5 键位映射表已完成验证，冲突全部解决

收到更新后的 `calibrated_keymap.json`，已独立核对：全部 68 个键的 LED ID 排序后
**没有重复**，此前发现的五组冲突（INS/K、DEL/L、PGUP/O/MINUS、BACKSPACE/P/EQUAL、
ENTER/I）全部通过重新标定得到了不同的、互不冲突的正确值。本次每条记录状态标记为
`MOUSE_CALIBRATED`（而非此前套公式生成的 `SWEEP_IDENTIFIED`），且校准时间戳按键
逐个间隔几秒钟递增，符合真实交互确认流程的时间特征，可信度显著高于此前版本。

**结论：这份键位映射表可以作为正式实现的基础数据。**

### 12.6 最终技术基础（供 Phase 1 实施使用）

- 控制通道：`AacKbHal_x64.dll`（`C:\Program Files\ASUS\Aac_Keyboard\`），
  `CLSID_ClaymoreHal = {AE9DB4C8-4F2A-4756-9B11-2F6D78C61F1A}`，
  `IID_IAsusAacLedDeviceHal = {F2C8D5B4-3854-4325-8A4F-FD7C5072E3BA}`
- 核心调用序列：`Access()`（hal_vtable[4]，锁定控制权）→ `CreateLedDevice()`
  （hal_vtable[5]，获取设备指针）→ 初始化 128 通道寻址表 → 持续以约 25 FPS 调用
  `Set_L_STD_SINGLE_XY`（dev_vtable[19]）推流
- 完整 68 键键位映射：见 `calibrated_keymap.json`（已验证无冲突）
- 不使用 WDL/LampArray（会导致奥创中心配置文件切换功能失效，用户已确认此取舍）
- 不使用 `ServiceMediator`、原始 HID 直连、`aura.sdk.1`/`IAuraSdk2`（均已证实
  对这把键盘不生效或不适用）

### 12.7 仍然待验证的工程问题（不影响架构基础，但实现时要处理）

- `CreateLedDevice` 用栈内存伪造 `std::vector<void*>` 传参的方式存在理论上的
  内存安全风险（如果底层触发扩容可能崩溃），C++ 正式实现时不应照搬这种手法，
  应该用真正安全的方式接收返回的设备指针（比如固定大小的输出缓冲区 + 显式长度，
  或者先用小规模测试确认底层实现的真实行为边界）
- 常驻 daemon 需要处理设备热插拔重连（双 PC 切换 USB-C 时）
- 需要验证长时间（几十分钟到几小时级别）持续 ~25FPS 推流的稳定性，不能只验证
  跑几秒钟不崩

### 端口分配（沿用此前报告建议，与 Aura 通道选择无关，继续有效）

- CS2 GSI 接收服务：`127.0.0.1:19897`
- 网页配置服务：`127.0.0.1:19898`

## 13. `CreateLedDevice` 初始化/释放循环确认存在真实内存泄漏

### 13.1 结论

拿到完整的 `--test-init 100` 日志（每 10 次一个采样点）后，通过分段计算增量确认：
`PrivateBytes` 从 Cycle 1 的 3.07MB 涨到 Cycle 100 的 44.30MB，**每个区间（1→10、
10→20、...、90→100）的增速稳定在 0.38~0.49 MB/次，从头到尾没有任何收敛趋平的
迹象**。如果是一次性开销（DLL 首次加载等），应该是前几次涨得多、后面摊平；这里
第 80~90 次和第 1~10 次的斜率几乎一样，是典型的线性内存泄漏特征，不是一次性
开销。此前报告用"100/100 成功、0 失败"来定性这次压测，掩盖了这条持续增长的曲线，
这个结论不成立，需要修正。

### 13.2 可能原因与修复方向

代码是直接操作 COM 对象的原始虚表指针调用（未走标准智能指针/`IUnknown` 引用计数
管理），大概率是每次循环释放阶段漏掉了对 `pDev` 和/或 `pHal` 调用其 vtable 里的
`Release()`（IUnknown 标准方法，通常在 index 2），导致对象引用计数没有正确递减、
内存没有真正归还。

### 13.3 影响范围与优先级

这条路径只在设备热插拔重连时触发（双 PC 切换），不是每帧都走。偶尔切换 PC 的
使用场景下短期内不会造成严重问题，但必须先修复再进入下一阶段，不能带着已知泄漏
往下推进；且这次的教训是：验收标准不能只看"是否报错/崩溃"，必须看资源占用的
增长曲线是否真的收敛。

### 13.4 待办

- [ ] 排查并修复 `pDev`/`pHal` 释放阶段缺失的 `Release()` 调用
- [ ] 修复后重新跑一次 `--test-init 100`，验收标准改为：`PrivateBytes` 曲线在
      最初一两次之后基本走平，不再是修复前那种全程稳定线性增长
- [ ] 30 分钟长效推流测试（持续推流、不反复 init/release）的结果本身是干净的，
      可以继续视为该路径本身没问题，不受本次发现影响，不需要重测

### 13.5 决定：暂缓修复，作为已知风险刻意接受

用户决定暂缓修复这个泄漏，进入下一阶段开发。理由：正常使用场景下（偶尔切换两台
PC）触发频率不足以在合理时间内积累成问题。**这是一个记录在案的、刻意接受的已知
风险，不是被遗漏或者遗忘的问题**——13.4 节的待办事项保留，后续有余力时应该修复，
但不阻塞 Phase 2 的开始。

## 14. Phase 2（轻量网页配置服务）基本完成

### 14.1 四点工程修正已落实

针对方案审计阶段提出的四个问题，实现报告给出的对应方案：

1. `TerminateProcess` 硬杀问题 → 改为命名事件通知子进程内部 `server.Stop()`
   平滑退出，Job Object 保留作为父进程崩溃时的内核级兜底，两层不冲突
2. 端口来不及释放问题 → 监听 socket 设置 `SO_REUSEADDR`
3. 主线程可能被 `CreateProcessW`/`TerminateProcess` 阻塞问题 → 移到独立专属
   工作线程，主线程只做耗时微秒级的信号触发
4. 启动失败无退避问题 → 连续失败 3 次后进入 30 秒静默退避

四点修正的方向都合理，直接对应此前提出的问题，方案本身没有发现新的设计漏洞。

### 14.2 待澄清的小问题（非阻塞）

- 报告里"平滑关闭耗时"的 1ms/14ms 不一致已有结论，不需要再问是不是测了两件 不同的事：README 附带的原始日志时间戳可以直接验算——从 `01:13:27.739`
 （发出通知）到 `01:13:27.753`（确认关闭）正好是 14ms，跟同一份文档里"1毫秒"的说法对不上。原始数据本身就能推出 14ms，"1毫秒"是文档撰写时的错误，需要把README 里"平滑关闭耗时: 1 毫秒"那一条改成跟日志一致的 14ms。快速切换压力测试只做了 3 次，样本量偏少，这正是最初担心的"端口来不及释放"
场景，后续有需要可以补充更高频次（20~30 次）的测试提高信心，目前不阻塞推进

- 快速切换压力测试只做了 3 次，样本量偏少，这正是最初担心的"端口来不及释放"
  场景，后续有需要可以补充更高频次（20~30 次）的测试提高信心，目前不阻塞推进

### 14.3 状态

Phase 2 基本完成，作为里程碑记录。下一阶段是 CS2 GSI 适配器。
README（含架构图、配置说明、构建步骤）已核对，除上述 1ms→14ms 的文档勘误外
没有发现新问题，可以视为 Phase 2 文档交付完成。
