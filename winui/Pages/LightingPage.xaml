<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Aura_WinUI.Pages.LightingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="Transparent"
    mc:Ignorable="d">

    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="24" Padding="36,24,36,36">
            <StackPanel Spacing="4">
                <TextBlock Text="灯效管理 (Lighting)" Style="{ThemeResource TitleTextBlockStyle}" />
                <TextBlock Text="逐键 RGB 灯效方案与硬件渲染参数"
                           Style="{ThemeResource BodyTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="20">
                <StackPanel Spacing="16">
                    <TextBlock Text="当前运行灯效参数" Style="{ThemeResource SubtitleTextBlockStyle}" />

                    <Grid ColumnSpacing="24" RowSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MinWidth="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="默认灯效" FontWeight="SemiBold" />
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="彩虹波浪 (Rainbow Wave) / 自定义图层" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="推流帧率" FontWeight="SemiBold" />
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="25 FPS (硬件节能防抖调度)" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="底层接口" FontWeight="SemiBold" />
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="Native Win32 HID 直接下发 (ROG Falchion Ace HFX)" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </Grid>
                </StackPanel>
            </Border>

            <InfoBar Title="第一阶段架构提示"
                     Message="本阶段保持 Native HID 控灯与 EffectEngine 核心完全独立运作。更高级的逐键可视化编辑与原生参数调节将在后续阶段通过 Control API 接入。"
                     IsOpen="True"
                     IsClosable="False"
                     Severity="Informational" />
        </StackPanel>
    </ScrollViewer>
</Page>
